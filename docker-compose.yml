services:
  vllm:
    image: vllm/vllm-openai:latest
    container_name: vllm-fluence
    ports:
      - "8000:8000"
    volumes:
      - ${HF_CACHE_DIR:-~/.cache/huggingface}:/root/.cache/huggingface
    command: [
      "--host", "0.0.0.0",
      "--port", "8000",
      "--model", "${VLLM_MODEL}",
      "--quantization", "${VLLM_QUANTIZATION}",
      "--max-model-len", "${VLLM_MAX_MODEL_LEN}",
      "--gpu-memory-utilization", "${VLLM_GPU_MEMORY_UTILIZATION}",
      "--hf-token", "${HF_TOKEN}",
      "--kv-cache-dtype","${KV_CACHE_DTYPE}",
      "--max-num-batched-tokens","${VLLM_MAX_BATCHED_TOKENS}",
      "--trust-remote-code",
      "--enable-prefix-caching"
    ]
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    healthcheck:
      test: [ "CMD-SHELL", "curl -fsS http://127.0.0.1:8000/health || exit 1" ]
      interval: 60s
      timeout: 3s
      retries: 6

  rabbitmq:
    image: rabbitmq:3.13-management
    container_name: rabbitmq-fluence
    ports:
      - "5672:5672"     # AMQP
      - "15672:15672"   # Web UI
    env_file:
      - rabbit/rabbit.env
    environment:
      RABBITMQ_LOAD_DEFINITIONS: "true"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq:rw
      - ./rabbit/definitions.json:/opt/definitions.json:ro
      - ./rabbit/rabbitmq.config:/etc/rabbitmq/rabbitmq.config:ro
    restart: unless-stopped

    healthcheck:
      test: [ "CMD", "rabbitmq-diagnostics", "check_port_connectivity" ]
      interval: 5s
      timeout: 10s
      retries: 5
      start_period: 10s

  api_gateway:
    build:
      context: services/api_gateway
      dockerfile: Dockerfile.api_gateway
    container_name: api_gateway
    env_file:
      - services/api_gateway/.env
    ports:
      - "8080:8080"
    restart: unless-stopped
    depends_on:
      rabbitmq:
        condition: service_healthy
  router:
    build:
      context: services/router
      dockerfile: Dockerfile.router
    container_name: router
    env_file:
      - services/router/.env
    volumes:
      - ./services/router/internal/routing_core/models:/app/internal/routing_core/models
      - ./services/router/internal/routing_core/data:/app/internal/routing_core/data
    depends_on:
      rabbitmq:
        condition: service_healthy
    restart: unless-stopped

  nmt_translator:
    build:
      context: services/machine_translator
      dockerfile: Dockerfile.machine_translator
    container_name: nmt_translator
    env_file:
      - .env
    ports:
      - "9090:9090"
    restart: unless-stopped
    depends_on:
      rabbitmq:
        condition: service_healthy
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]

  llm_translator:
      build:
        context: services/llm_translator
        dockerfile: Dockerfile.llm_translator
      container_name: llm_translator
      env_file:
        - .env
      ports:
        - "9091:9090"
      restart: unless-stopped
      depends_on:
        rabbitmq:
          condition: service_healthy
        vllm:
          condition: service_healthy

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    ports:
      - "9092:9090"
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./monitoring/prometheus/rabbitmq_rules.yml:/etc/prometheus/rabbitmq_rules.yml:ro
      - ./monitoring/prometheus/router_intelligence_rules.yml:/etc/prometheus/router_intelligence_rules.yml:ro
      - prometheus_data:/prometheus
    restart: unless-stopped

  grafana:
    image: grafana/grafana:latest
    container_name: grafana-fluence
    ports:
      - "3000:3000"
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_USERS_ALLOW_SIGN_UP: "false"
    volumes:
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./monitoring/grafana/dashboards:/etc/grafana/dashboards:ro
      - grafana_data:/var/lib/grafana
    depends_on:
      - prometheus
    restart: unless-stopped

networks:
  fluence-net:
    name: test-net
    driver: bridge

volumes:
  rabbitmq_data:
  prometheus_data:
  grafana_data:
